spring:
  data:
    mongodb:
      host: mongodb
      port: 27017
      database: event_metadata
      repositories:
        enabled: true
      auto-index-creation: true

server:
  error:
    include-message: always

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG