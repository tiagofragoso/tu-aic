const { Router } = require("express");

const router = Router();

router.get("/:id", (_req, _res, _next) => {
    // Do something
});

router.post("/", (_req, _res, _next) => {
    // Do something
});

module.exports = router;
